-- =====================================================================
-- KLONGZAND PMS - Booking Documents (เพิ่มเติม)
-- รันไฟล์นี้ใน Supabase SQL Editor ต่อจากไฟล์เดิม (ไม่กระทบตารางเดิม)
-- เพิ่มช่องเก็บลิงก์รูปบัตรประชาชน / ทะเบียนรถ ในการจอง (ใส่หรือไม่ใส่ก็ได้)
-- =====================================================================

alter table bookings add column if not exists id_card_url text;       -- ลิงก์รูปบัตรประชาชน (ไม่บังคับ)
alter table bookings add column if not exists vehicle_reg_url text;   -- ลิงก์รูปทะเบียนรถ (ไม่บังคับ)

-- =====================================================================
-- หมายเหตุ: ต้องสร้าง Storage bucket ชื่อ "booking-documents" ใน Supabase Dashboard
-- (Storage > New bucket) ด้วยตนเองก่อน ดูวิธีในข้อความคำแนะนำที่แนบมา
-- =====================================================================
