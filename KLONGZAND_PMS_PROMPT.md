# PROMPT สำหรับ Claude ใน VS Code — สร้างระบบ KLONGZAND PMS

คัดลอกข้อความทั้งหมดด้านล่างนี้ วางใน Claude (VS Code extension) ที่ root ของ repo โปรเจกต์

---

ฉันต้องการให้คุณสร้างเว็บแอประบบจัดการรีสอร์ทชื่อ **"KLONGZAND PMS"** ให้ครบทั้งระบบ ในโปรเจกต์นี้ (repo นี้คือ GitHub repo ที่เชื่อม Vercel ไว้แล้ว: https://github.com/shawakorn35v2-lab/KLONGZAND-PMS.git ) กรุณาทำตามสเปกด้านล่างทั้งหมด

## 1. Tech Stack
- Next.js (App Router, JavaScript, ไม่ใช้ TypeScript)
- Tailwind CSS สำหรับ styling
- Supabase สำหรับ Database + Auth (ใช้ `@supabase/supabase-js` และ `@supabase/ssr`)
- Deploy บน Vercel (โค้ดต้อง build ผ่านด้วย `next build` ไม่มี error)
- ใช้ `recharts` สำหรับกราฟในหน้า Dashboard
- ภาษา UI ทั้งหมดเป็น**ภาษาไทย** แสดงตัวเลขเงินเป็นหน่วยบาท

## 2. Database — ใช้ schema ที่มีอยู่แล้ว ห้ามสร้างตารางใหม่เอง
ฉันรัน SQL schema ไว้ใน Supabase แล้วจากไฟล์ `supabase_schema.sql` และ `supabase_schema_inventory.sql` (อยู่ใน repo นี้ที่ root) ให้คุณอ่านไฟล์ทั้งสองเพื่อเข้าใจโครงสร้างตารางทั้งหมดก่อนเขียนโค้ด ตารางที่มี:

- `profiles` (id, full_name, role: 'admin' | 'staff')
- `rooms` (room_no, building, room_type, price_per_night, is_monthly, monthly_rent, housekeeping_status: 'clean'|'dirty'|'maintenance', is_active) — มีข้อมูล 19 ห้องอยู่แล้ว: A1-A7, B1-B6, C1-C6
- `customers` (full_name, phone, note)
- `bookings` (room_id, customer_id, channel: 'walkin'|'agoda'|'line'|'facebook'|'sale', checkin_date, checkout_date, price, deposit, status: 'reserved'|'checked_in'|'checked_out'|'cancelled', note)
- `transactions` (tx_date, tx_type: 'income'|'expense', category, amount, note, booking_id) — คอลัมน์ `is_closed` ยังคงอยู่ในสคีมาแต่ **ไม่ได้ใช้แล้ว** (ฟีเจอร์ปิดยอดถูกถอดออก 2026-07-02)
- `daily_closings` — ตารางยังคงอยู่ในสคีมาแต่ **ไม่มีโค้ดเรียกใช้แล้ว** (ฟีเจอร์ปิดยอดถูกถอดออก 2026-07-02)
- `meter_readings` (room_id, billing_month, prev_reading, curr_reading, unit_price, water_flat_fee, electric_cost [generated], total_amount [generated]) — ใช้เฉพาะห้องที่ `is_monthly = true`
- `housekeeping_log` (room_id, status, note, updated_by, updated_at)
- `inventory_items` (name, unit, current_stock, reorder_point) — รายการของใช้ (น้ำดื่ม, สบู่, แชมพู, กระดาษทิชชู ฯลฯ) เพิ่มรายการใหม่ได้ไม่จำกัด
- `inventory_movements` (item_id, movement_type: 'stock_in'|'stock_out', quantity, room_id [ใช้เฉพาะ stock_out เบิกไปใช้ห้องไหน], unit_cost [ใช้เฉพาะ stock_in], note) — มี trigger อัปเดต current_stock อัตโนมัติ ไม่ต้องคำนวณซ้ำใน frontend
- `inventory_requests` (item_id, requested_qty, status: 'pending'|'approved'|'fulfilled'|'rejected', requested_by, resolved_by) — คำขอของใช้เพิ่มเติม

## 3. Environment Variables
มีไฟล์ `.env.local.example` อยู่แล้วใน repo ให้ใช้ตัวแปรชื่อ `NEXT_PUBLIC_SUPABASE_URL` และ `NEXT_PUBLIC_SUPABASE_ANON_KEY` ในการสร้าง Supabase client (อย่าฮาร์ดโค้ดค่าใด ๆ ไว้ในโค้ด)

## 4. Authentication & สิทธิ์ผู้ใช้งาน
- หน้า `/login` ใช้ Supabase Auth (`signInWithPassword`) — อีเมล/รหัสผ่าน
- สร้าง `middleware.js` ป้องกันทุก route ยกเว้น `/login` — ถ้ายังไม่ login ให้ redirect ไป `/login`
- ดึง `role` จากตาราง `profiles` ของผู้ใช้ที่ login:
  - **admin**: เข้าถึงได้ทุกหน้า ทุกฟีเจอร์ รวมรายงานการเงินทั้งหมด
  - **staff**: เข้าถึงหน้าจองห้อง, ลูกค้า, กรอกรายรับ-รายจ่ายประจำวัน, ทำความสะอาดห้องได้ แต่**ดูรายงานสรุปการเงินรวม/Dashboard เชิงลึกไม่ได้** (ซ่อนหรือ disable เมนูเหล่านั้น); **staff โยกห้อง (แก้ไขการจอง/เปลี่ยนห้อง) ได้** แต่ลบการจองไม่ได้
- มีปุ่ม Logout ใน layout หลัก

## 5. หน้าเว็บที่ต้องสร้าง

### `/dashboard` (หน้าแรกหลัง login)
- สรุปยอดรายรับ / รายจ่าย / กำไรสุทธิ ของวันนี้ และเดือนนี้
- สถานะห้องพักวันนี้ (ว่าง / มีลูกค้าเข้าพัก / ทำความสะอาด) แสดงเป็น grid 19 ห้อง พร้อมสีบอกสถานะ
- กราฟยอดขายรายเดือน (แท่งหรือเส้น) ใช้ recharts ดึงจาก `transactions` ที่ `tx_type = 'income'`
- กราฟ/ตารางสรุปยอดขายแยกตามช่องทาง (walk-in / Agoda / Line / Facebook / Sale) จากตาราง `bookings.channel`
- Occupancy rate (% ห้องที่มีลูกค้าวันนี้ / 19 ห้อง)

### `/rooms` (จัดการห้องพัก)
- ตารางแสดง 19 ห้อง พร้อมแก้ไขราคา/ประเภท/สถานะแม่บ้านได้ (admin เท่านั้นแก้ราคา)
- toggle เปิด/ปิด `is_monthly` ต่อห้อง พร้อมตั้งค่า `monthly_rent`
- ลิงก์ไปหน้ามิเตอร์ของห้องที่เป็นรายเดือน

### `/rooms/[id]/meter` (มิเตอร์ไฟ + ค่าน้ำเหมา — เฉพาะห้องรายเดือน)
- กรอกเลขมิเตอร์ก่อน-หลังของแต่ละเดือน, ตั้งราคาต่อหน่วย, ค่าน้ำเหมา
- แสดงผลคำนวณค่าไฟ และยอดรวมทั้งหมด (ใช้ generated columns ที่มีอยู่แล้วในฐานข้อมูล ไม่ต้องคำนวณซ้ำใน frontend)
- ประวัติมิเตอร์ย้อนหลังของห้องนั้น (list ทุกเดือน)

### `/bookings` (จองห้องพัก)
- ฟอร์มสร้างการจองใหม่: **ประเภทการพัก** radio — "พักค้างคืน" (stay_type='overnight', checkin/checkout date แบบเดิม) หรือ "พักชั่วคราว" (stay_type='temporary', date + checkin_time + checkout_time วันเดียวกัน)
- ห้องเดียวกันรับ **หลายรอบชั่วคราว** ในวันเดียวกันได้ (ตราบเท่าที่เวลาไม่ทับซ้อน); server-side conflict check ใน `createBooking` (`app/actions/bookings.js`)
- ปฏิทิน/ตารางห้องว่าง: แสดง 7 วัน, **วันนี้อยู่กลาง (offset -3)**, เลื่อนซ้าย-ขวาได้ทีละ 7 วัน, ปุ่ม "วันนี้" reset กลับ; temporary booking ใช้ checkin_date เดียวกันสำหรับการแรเงา
- **กรองช่วงวันที่** ในรายการจอง (dateFrom/dateTo กรอง checkin_date ที่ server)
- ปุ่มเช็คอิน / เช็คเอาท์ เปลี่ยนสถานะ booking และอัปเดตสถานะห้อง (housekeeping_status เป็น 'dirty' หลังเช็คเอาท์)
- เมื่อเช็คอิน/ยืนยันชำระเงิน ให้สร้างรายการใน `transactions` (tx_type='income') อัตโนมัติ ผูกกับ `booking_id`
- **Admin/Staff แก้ราคา/มัดจำ/โยกห้อง**: `adminUpdateBooking` (`app/actions/bookings.js`) อนุญาต role 'admin' และ 'staff' — อัปเดต transaction amounts อัตโนมัติ (ค่ามัดจำ = deposit ใหม่, ค่าห้อง = price - deposit ใหม่) — ป้องกัน A2-style desync; staff เห็นปุ่ม ✏ แก้ไข แต่ไม่เห็นปุ่ม 🗑 ลบ
- **Admin ลบการจอง**: ลบ transactions ทั้งหมดที่ผูกกับ booking ก่อนลบ booking
- **checkin_time/checkout_time**: ฟิลด์ type `time` ใน DB — ต้องส่งเป็น `null` (ไม่ใช่ `""`) สำหรับ stay_type='overnight'; `openEdit` ใน BookingsClient.js initialize ด้วย `?? null`; server action ใช้ `stay_type === 'temporary' ? (value || null) : null` เป็น guard

### `/customers` (ลูกค้า + ประวัติ)
- รายชื่อลูกค้าทั้งหมด ค้นหาได้ด้วยชื่อ/เบอร์โทร
- หน้ารายละเอียดลูกค้า แสดงประวัติการเข้าพักทั้งหมด (join จาก `bookings`)

### `/transactions` (รายรับ-รายจ่ายประจำวัน + บันทึกขายของ)
- ฟอร์มกรอกรายรับ/รายจ่ายเอง (เช่น ค่าน้ำไฟ ค่าซ่อมบำรุง ค่าใช้จ่ายอื่น ๆ) พร้อมหมวดหมู่
- รายการของวันนี้ พร้อมยอดรวมรายรับ/รายจ่าย/กำไรสุทธิแบบ real-time
- **ไม่มีระบบปิดยอด** — เพิ่ม/ลบรายการได้ตลอด (สิทธิ์แก้ไข/ลบยึดตาม role ปกติผ่าน RLS: staff เห็นเฉพาะของตัวเอง, admin เห็นทุกคน) — เดิมมีปุ่มปิดยอดประจำวันแต่ถอดออก 2026-07-02
- ตัวกรองดูย้อนหลังตามช่วงวันที่ พร้อมยอดสรุป
- ตารางมีคอลัมน์ **"ห้อง"** — query ด้วย `select('*, bookings(room_id, rooms(room_no))')` แล้ว flatten เป็น `room_no` ก่อนส่งไป client; แสดงเลขห้องสำหรับรายการที่มี `booking_id` และ `—` สำหรับรายการที่ไม่มี (รายจ่ายทั่วไป, ขายของ)
- รายการอัตโนมัติจากการจอง/เช็คอินมีเลขห้องในช่อง `note` ด้วย เช่น `มัดจำการจองห้อง ห้อง A2`, `รับเงินเช็คอิน ห้อง A2 (ส่วนที่เหลือ)` — สร้างใน `app/actions/bookings.js` (`createBooking` และ `checkinBooking`)

### `/housekeeping` (สถานะแม่บ้าน/ทำความสะอาด)
- รายการห้องทั้งหมดพร้อมสถานะปัจจุบัน (clean/dirty/maintenance)
- กดเปลี่ยนสถานะได้ ทุกครั้งที่เปลี่ยนให้บันทึก log ลง `housekeeping_log`
- ดูประวัติการเปลี่ยนสถานะของแต่ละห้องได้

### `/inventory` (สต๊อกของใช้)
- หน้าหลัก: รายการของใช้ทั้งหมด (เริ่มมี น้ำดื่ม, สบู่, แชมพู, กระดาษทิชชู — เพิ่ม/แก้ไข/ลบรายการใหม่ได้ไม่จำกัด) แสดงจำนวนสต๊อกปัจจุบัน (`current_stock`) ไฮไลท์สีแดงถ้าต่ำกว่า `reorder_point`
- ฟอร์ม **"ซื้อของเข้า/รับเข้าสต๊อก"**: เลือกของใช้, จำนวน, ราคาต่อหน่วย, บันทึก → insert ลง `inventory_movements` (movement_type='stock_in')
- ฟอร์ม **"เบิกของไปใช้"**: เลือกของใช้, จำนวน, **เลือกห้องที่เบิกไปใช้** (dropdown 19 ห้อง, เลือกไม่เจาะห้องก็ได้), บันทึกผู้เบิก → insert ลง `inventory_movements` (movement_type='stock_out')
- ตารางประวัติการเบิก/รับเข้าทั้งหมด กรองตามช่วงวันที่ / ห้อง / รายการของใช้ได้
- ฟอร์ม **"ขอของใช้เพิ่มเติม"**: พนักงานเลือกของใช้ที่ใกล้หมด+จำนวนที่ขอ → สร้าง record สถานะ 'pending' ใน `inventory_requests` — แอดมินกดเปลี่ยนสถานะเป็น approved/fulfilled/rejected ได้ในหน้าเดียวกัน

### Export รายงาน (Excel/PDF)
- ในหน้า `/transactions` และ `/dashboard` ให้มีปุ่ม "Export Excel" และ "Export PDF" สำหรับสรุปรายรับ-รายจ่ายตามช่วงวันที่ที่เลือก
- ใช้ไลบรารี `xlsx` (SheetJS) สำหรับ Excel และ `jspdf` + `jspdf-autotable` สำหรับ PDF (ฝั่ง client-side พอ ไม่ต้องทำ server-side)

## 6. UI/Layout
- มี sidebar navigation ถาวร: Dashboard, จองห้องพัก, ห้องพัก, ลูกค้า, รายรับ-รายจ่าย, ทำความสะอาด (และซ่อนเมนูที่ staff ไม่มีสิทธิ์)
- Responsive ใช้งานบนจอคอมพิวเตอร์และแท็บเล็ตได้ดี
- ใช้สีและ badge บอกสถานะห้อง/การจองให้ดูง่าย (เขียว=ว่าง, แดง=ไม่ว่าง, เหลือง=ทำความสะอาด)

## 7. ข้อกำหนดทางเทคนิค
- ทุก Server Action ให้ใช้ `'use server'` และ `revalidatePath()` หลัง mutation
- ทุก Client Component ที่กดปุ่มแล้วเรียก Server Action ให้ใช้ `router.refresh()` เพื่อ sync ข้อมูล
- ห้าม hardcode ค่า environment variable ใดๆ ในโค้ด
- ทุกหน้าต้อง build ผ่านด้วย `next build` ไม่มี error
- UI ทั้งหมด responsive รองรับมือถือ (md: breakpoint สำหรับ sidebar)

## 8. สถานะที่สร้างแล้ว (อัปเดต 2026-07-02)
หน้าและฟีเจอร์ที่สร้างเสร็จแล้วในโปรเจกต์นี้:
- ✅ `/login` — Supabase Auth login
- ✅ `/dashboard` — สรุปยอดวันนี้/เดือนนี้, RoomGrid, กราฟ recharts, Export Excel/PDF, ภาพรวมสะสมทั้งหมด (all-time income/expense/net/booking count), Occupancy เรียลไทม์ (date-based: checkin_date ≤ today < checkout_date หรือ is_monthly room), Occupancy รายเดือน (dropdown เลือกเดือน, แสดงห้อง-คืน, คำนวณจาก bookings + monthly rooms ทุกวันในเดือน), กราฟอัตราเข้าพักรายเดือน 12 เดือน (BarChart)
- ✅ `/bookings` — จอง/เช็คอิน/เช็คเอาท์, ตารางห้องว่าง 7 วัน, ค้นหาห้องว่างตามช่วงวันที่, อัปโหลดบัตรประชาชน/ทะเบียนรถ (Supabase Storage bucket `booking-documents`); **คลิกช่องสีแดงในตารางสถานะห้อง 7 วัน** → popup โชว์รายละเอียดการจองของห้อง-วันนั้น (ชื่อลูกค้า, เบอร์, เช็คอิน/เช็คเอาท์ + เวลา ถ้า temporary, ราคา, มัดจำ, ช่องทาง, สถานะ, หมายเหตุ) + ปุ่ม "✏ แก้ไขการจองนี้" เรียก `openEdit(b)` เดิมเปิด edit modal ทันที; รองรับหลาย bookings ต่อ cell (เช่น overnight+temporary ทับวันเดียวกัน) ด้วย `occupiedMap` คู่กับ `occupiedSet` ใน `BookingsClient.js`; **จองหลายห้องในชื่อลูกค้าเดียว**: `BookingForm.js` เปลี่ยนจาก `form.roomId/price/deposit` (เดี่ยว) → `form.rooms: [{roomId, price, deposit}]` (array) — ลูกค้า/วันที่/ช่องทาง/เอกสาร share ทุกห้อง, ราคา+มัดจำกรอกแยกต่อห้อง, ปุ่ม "+ เพิ่มห้อง"/"ลบห้องนี้", dropdown แต่ละแถวกรองห้องที่เลือกในแถวอื่นออก; server action ใหม่ `createMultiBookings` (`app/actions/bookings.js`) — pre-flight conflict check ทุกห้องด้วย batch query (`.in('room_id', roomIds)` + overlap), ถ้ามีห้องไม่ว่างยกเลิกทั้งหมดทันที (return error พร้อมเลขห้องที่ชน); ผ่านแล้ว find-or-create customer ครั้งเดียว, loop insert N bookings + N transaction มัดจำ ผูก `customer_id` เดียวกัน; ถ้า insert พังกลางทาง rollback ลบ transactions+bookings ที่สร้างไปแล้วทั้งหมด (best-effort เพราะ Supabase JS ไม่รองรับ DB transaction); `createBooking` เดิมไม่แตะ — admin edit modal และ flow อื่นใช้ตามเดิม
- ✅ `/customers` — รายชื่อลูกค้า, ค้นหา, ประวัติการเข้าพัก
- ✅ `/transactions` — รายรับ-รายจ่าย, Export, ปุ่ม "🛒 บันทึกขายของ" (เลือกสินค้าจาก is_for_sale=true, แสดงสต๊อก+ราคา, คำนวณยอดอัตโนมัติ, กันเกินสต๊อก, เรียก sellItem action เดิม — atomic transaction+movement, revalidate /inventory+/dashboard อัตโนมัติ); **ลบรายการขายของ**: `deleteTransaction` (`app/actions/transactions.js`) ก่อนลบ transaction จะ query `inventory_movements` ที่ `transaction_id` ตรงกัน → คืนสต๊อก (`current_stock` +=qty สำหรับ sale/stock_out, -=qty สำหรับ stock_in) → ลบ movements → ลบ transaction แล้วค่อย revalidate /inventory; UI (`TransactionsClient.js`) ตรวจ `category === 'ขายของ'` แสดง popup เตือนต่างหากก่อนลบเสมอ ครอบคลุม movement_type ทุกประเภทที่ผูกกับ transaction (ไม่ใช่เฉพาะ sale); **Role-based view (staff = เฉพาะของตัวเอง)**: ใน `app/(protected)/transactions/page.js` fetch `profiles.role` ก่อน แล้วใส่ `.eq('created_by', user.id)` เข้า transactions query เมื่อ role !== 'admin' — การ์ดสรุปวันนี้ (รายรับ/รายจ่าย/กำไรสุทธิ), ตารางรายการ, ExportButtons (Excel/PDF), และ date-range filter ทั้งหมด inherit การกรองนี้อัตโนมัติเพราะมาจาก array `transactions` ตัวเดียวกัน; แถวสรุปยอดช่วงวันที่ใต้ date filter (`รายรับรวม / รายจ่ายรวม / กำไรสุทธิ`) wrap ด้วย `{isAdmin && (...)}` ใน `TransactionsClient.js` — ซ่อนสำหรับ staff เพราะเป็นภาพรวมที่ไม่ต้องการให้เห็น; admin ยังเห็นข้อมูลครบเหมือนเดิมทุกอย่าง
- ✅ `/housekeeping` — สถานะห้อง, เปลี่ยนสถานะ, log
- ✅ `/rooms` — จัดการห้อง 19 ห้อง, toggle monthly
- ✅ `/rooms/[id]/meter` — มิเตอร์ไฟ/ค่าน้ำ รายเดือน, Export PDF
- ✅ `/inventory` — 5 แท็บ: **สต๊อก** (แสดงทุกรายการทั้ง is_for_sale=true/false/null รวมกัน, badge "ขายได้" ต่อท้ายชื่อถ้า is_for_sale=true, admin เพิ่ม/ตั้งค่า), **รับ/เบิก** (stock_in/stock_out ทุกรายการทั้ง sale และ non-sale — ของชิ้นเดียวกันใช้สต๊อกร่วมกันได้ทั้ง 2 ทาง), **ขายของ** (กรองแสดงเฉพาะ is_for_sale=true — "+ เพิ่มสินค้าขาย" เช็คชื่อซ้ำก่อน insert: ถ้าซ้ำแต่ยังไม่ใช่ sale item popup ถามยืนยัน, ถ้าซ้ำและเป็น sale item แล้วแจ้งเตือน; ปุ่ม "รับเข้า" per-row, ปุ่ม "ขาย" per-row — atomic transaction+movement), **คำขอ** (inventory_requests, admin อนุมัติ/ปฏิเสธ), **ประวัติ** (ทุกประเภท, badge สีเข้ม 3 สี); กันเกินสต๊อก client+server; rollback transaction ถ้า movement insert ล้มเหลว
- ✅ Sidebar responsive, hamburger menu บนมือถือ
- ✅ ทุกหน้า responsive (p-4 md:p-6, grid-cols-1 sm:grid-cols-X)
- ✅ `lib/dateUtils.js` — utility กลาง: formatDate (DD/MM/พ.ศ.), formatMonth, formatShortDate, formatLongDate, formatDateTime (timestamp + เวลา ไทม์โซน Asia/Bangkok), getTodayString (local timezone safe)
- ✅ `lib/currency.js` — `roundCurrency(value)` ปัดเศษเงินเป็น 2 ตำแหน่งแบบกัน floating-point error (`Math.round((n + Number.EPSILON) * 100) / 100`); ใช้ทุกจุดที่เขียน price/deposit ลง `bookings` และคำนวณ remaining/transaction amount (`createBooking`, `checkinBooking`, `adminUpdateBooking` ใน `app/actions/bookings.js`) กันราคา 300 → 299.99 จาก FP noise
- ✅ ฟอร์ม number input ของราคา/มัดจำ (BookingForm + edit modal ใน BookingsClient) เพิ่ม `onWheel={e => e.currentTarget.blur()}` กันลูกกลิ้งเมาส์เปลี่ยนค่าโดยไม่ตั้งใจ (step="0.01" + scroll = ลด 0.01 อัตโนมัติ)
- ✅ `supabase_audit_temp_price.sql` — query แบบอ่านอย่างเดียว นับ/list รายการ stay_type='temporary' ที่ price/deposit ลงท้ายด้วย .99/.98/.01/.02 (น่าจะตั้งใจเป็น .00) สำหรับตรวจสอบข้อมูลเก่าก่อนแก้ไขด้วยตัวเอง
- ✅ ฟิลด์ "จำนวน" ในทุกฟอร์มของ `/inventory` (ขายของ, รับเข้าสต๊อก sale-item, รับเข้าสต๊อกทั่วไป, เบิกของ) เปลี่ยนจาก `step="0.01" min="0.01"` เป็น `step="1" min="1" inputMode="numeric"` พร้อม `onWheel blur` กันบั๊กพิมพ์ "2" แล้วบันทึกเป็น 0.02 (iOS Safari/Chrome mobile กับ step="0.01" แปล digit เป็นช่องทศนิยมก่อน); ราคา/ทุนต่อหน่วยยังเป็น step="0.01" ตามเดิม
- ✅ `handleSell` ใน `InventoryClient.js` เพิ่ม confirm popup ถ้าจำนวนที่กรอก < 1 (เช่น 0.02) — แสดงจำนวน × ราคา × ยอดรวม ก่อนยืนยัน กันเคสบันทึกจำนวนผิดโดยไม่ตั้งใจ
- ✅ **onWheel blur ครอบคลุมทุก `<input type="number">`** — กันเคส scroll wheel เลื่อนค่าโดยไม่ตั้งใจ (เช่น ราคา 550 → 549.99) ครอบคลุมทุกฟอร์มเงิน/ราคา/จำนวน: `BookingForm` (price, deposit), `BookingsClient` edit modal (price, deposit), `TransactionForm` (amount), `TransactionsClient` sell modal (quantity — เปลี่ยน step=1 min=1 inputMode=numeric ด้วย), `MeterForm` (prev/curr_reading, unit_price, water_flat_fee), `MeterClient` (rentVal, edit prev/curr_reading, unit_price, water_flat_fee, deduction.amount), `RoomsTable` EditableCell (price_per_night, monthly_rent), `InventoryClient` ทุกช่อง (reorder_point, quantity ทั้ง 4, unit_cost ทั้ง 2, sale_price ทั้ง 2, requested_qty)
- ✅ **ฟอนต์ระบบเปลี่ยนเป็น Mali** (ฟอนต์ลายมือไทยจาก Google Fonts) — โหลดผ่าน `next/font/google` ใน `app/layout.js` (subsets: thai+latin, weight: 400/500/600/700, `variable: '--font-mali'`, `display: 'swap'`), ผูก `className={mali.variable}` ที่ `<html>`; `tailwind.config.js` ตั้ง `fontFamily.sans: ['var(--font-mali)', 'system-ui', '-apple-system', 'sans-serif']` ให้ Tailwind `font-sans` ทั้งระบบใช้ Mali อัตโนมัติ; ลบ Sarabun `@import` เดิมใน `app/globals.css` ออก (ย้ายมา self-hosted ตามแนวทาง Next.js 15 — ไม่มี render-blocking, ไม่ layout shift)
- ✅ **โลโก้ระบบใช้รูปจริง** (แทน emoji 🏖️ เดิม): `public/logo-icon.png` (ไอคอนเดี่ยว ไม่มีตัวหนังสือ) + `public/logo-full.png` (โลโก้เต็มมี "KLONGZAND PMS" + "PROPERTY MANAGEMENT SYSTEM") — หน้า login (`app/login/page.js`) ใช้ `<Image src="/logo-full.png" priority>` (next/image) แทน emoji+h1+subtitle เพราะข้อความอยู่ในรูปแล้ว; sidebar header (`components/Sidebar.js`) ใช้ `<Image src="/logo-icon.png" width={40} height={40}>` คู่กับข้อความ "KLONGZAND / PMS"; mobile topbar (`components/LayoutShell.js`) ใช้ `<Image width={28} height={28}>` คู่กับข้อความ "KLONGZAND PMS"; ทุกจุดใช้ `next/image` เพื่อ optimize เป็น webp อัตโนมัติ
- ✅ **PWA support** (เพิ่มลงหน้าจอโฮมได้บน Android Chrome + iPhone Safari): ไอคอน 3 ไฟล์ใน `public/` — `icon-192.png` (192×192), `icon-512.png` (512×512), `apple-touch-icon.png` (สำหรับ iOS home screen); `public/manifest.json` — name "KLONGZAND PMS" / short_name "KLONGZAND" / start_url `/dashboard` / display `standalone` / theme+background `#001840` / icons array 4 entries (192+512 × any+maskable purpose); `middleware.js` matcher เพิ่ม `manifest.json|manifest.webmanifest|` เข้า negative-lookahead ให้ middleware ไม่ rewrite/auth manifest (ไอคอน .png ถูกยกเว้นโดย matcher เดิมอยู่แล้ว); `app/layout.js` ใช้ Next.js 15 metadata API — `metadata.manifest = '/manifest.json'`, `metadata.icons.icon = ['/icon-192.png', '/icon-512.png']` (แทน `/logo-icon.png` เดิม), `metadata.icons.apple = '/apple-touch-icon.png'`, แยก `export const viewport = { themeColor: '#001840' }` ตาม convention ใหม่ของ Next.js 15; ไม่มี service worker / offline cache (ตามที่ผู้ใช้กำหนด — แค่ "เพิ่มลงหน้าจอโฮม" พอ)
- ✅ **ปุ่ม "✏ แก้ไข" ในหน้า `/inventory`** (2026-07-02, admin only): เพิ่มปุ่มสีน้ำเงินในคอลัมน์การดำเนินการทั้งในแท็บ "สต๊อก" และ "ขายของ" ข้าง `⚙ ตั้งค่า` — เปิด modal `editCoreItem` แก้ไขได้ 3 ฟิลด์: `name` (ชื่อรายการ), `unit` (หน่วย), `reorder_point` (จุดเตือนสต๊อก); กด "บันทึก" → เรียก `updateInventoryItem(id, {name, unit, reorder_point})` ที่ขยายให้รับฟิลด์เพิ่มแบบ partial patch (แยก key ด้วย `'name' in fields` เพื่อไม่ทับ `is_for_sale`/`sale_price` ที่ modal "ตั้งค่า" ยังใช้อยู่); server action ตรวจ role='admin' + trim/validate ชื่อและหน่วยว่าไม่ว่าง; UI ปุ่มซ่อนสำหรับ staff (wrap ด้วย `{isAdmin && ...}` แบบเดียวกับปุ่มตั้งค่า)
- ✅ **ระบบออกใบเสร็จรับเงิน `/receipts`** (2026-07-03, admin + staff ใช้ได้): ตาราง `receipts` (id, receipt_no unique, booking_id nullable, customer_* fields, payment_method check-in `('cash','transfer','other')`, payment_other_note, remark, subtotal/discount/total_amount numeric, created_by, created_at) + `receipt_items` (receipt_id FK cascade, sort_order, description, quantity, unit_price, amount) — schema จาก `supabase_schema_receipts.sql` (RLS enabled: select/insert=authenticated, update=admin-or-creator, delete=admin); 3 route ใหม่: **`/receipts`** ลิสต์ + search + pagination (20/หน้า) + ปุ่ม 📄 ดู/พิมพ์ + ปุ่ม 🗑 ลบ (admin only), **`/receipts/new`** ฟอร์มสร้าง (ข้อมูลลูกค้า + ตารางรายการเริ่ม 3 แถวเพิ่มได้ถึง 10 + คำนวณ subtotal/total อัตโนมัติ + radio ชำระโดย + ช่องหมายเหตุ + ช่องส่วนลด — บันทึกแล้ว redirect ไป `/receipts/[id]`), **`/receipts/[id]`** preview พิมพ์ได้เต็มรูปแบบตามแบบฟอร์ม KLONG ZAND RESORT PHALA (โลโก้ + "ใบเสร็จรับเงิน / RECEIPT" + เลขที่/วันที่ + ข้อมูลลูกค้า + ตารางรายการ 10 แถวเส้นสีเขียวเข้ม `#2d5016` header สีเขียวขาว + checkbox ชำระโดย + สรุปยอด "รับเงินแล้ว" สีเขียว + ลายเซ็นผู้รับ/ผู้ชำระ + ท้ายกระดาษ "ขอบคุณที่ใช้บริการ / THANK YOU FOR YOUR SUPPORT"); ปุ่ม **"🖨 พิมพ์/ดาวน์โหลด PDF"** ใช้ html2canvas snapshot `receiptRef` (A4 210×297mm) → jsPDF embed PNG — ภาษาไทยครบด้วยเหตุผลเดียวกับใบแจ้งหนี้มิเตอร์ (`<img>` ตรงไป ไม่ใช้ next/image เพื่อกัน lazy-load ทำ canvas capture ก่อนภาพโหลด); server action `app/actions/receipts.js`: **`getReceipts({page, limit, search})`** ค้นด้วย `.or('receipt_no.ilike.%s%,customer_name.ilike.%s%')` (escape `%_,`), **`getReceipt(id)`** parallel select receipt + items เรียงตาม sort_order, **`createReceipt(payload)`** สร้าง `receipt_no = RC-YYYYMMDD-NNN` (`YYYYMMDD` Bangkok TZ ผ่าน `Intl.DateTimeFormat('en-CA')` + count existing rows ที่ `like RC-{date}-%`) กับ retry loop สูงสุด 5 ครั้งกัน race — insert receipts ก่อนแล้ว insert items batch, ถ้า items ล้ม rollback ลบ receipt (best-effort), **`deleteReceipt(id)`** admin only + cascade ลบ items อัตโนมัติจาก FK; เมนูใหม่ **"🧾 ออกบิล"** ใน `Sidebar.js` วางหลัง "รายรับ-รายจ่าย" `adminOnly: false` — ทั้ง admin + staff ออกบิลได้
- ✅ **เมนู "ห้องพัก" ในไซด์บาร์เปิดให้ staff เห็นแล้ว** (2026-07-03): `components/Sidebar.js` เปลี่ยน `{ href: '/rooms', ..., adminOnly: true }` → `adminOnly: false` — staff จะเห็นเมนู "ห้องพัก" ในไซด์บาร์เหมือน admin แล้ว **หมายเหตุ:** `middleware.js` ยังมี `/rooms` ใน `ADMIN_ONLY_PATHS` อยู่ (line 4) ถ้า staff คลิกเข้าไปจะโดน redirect ไป `/bookings` — ถ้าต้องการให้ staff เข้าใช้งานหน้าจริงต้องเอา `/rooms` ออกจาก `ADMIN_ONLY_PATHS` ในไฟล์ middleware ด้วย
- ✅ **PDF ใบแจ้งหนี้ห้องรายเดือน — ภาษาไทยแสดงถูกต้องแล้ว** (2026-07-03, fix): เดิม `handlePrintPDF` ใน `app/(protected)/rooms/[id]/meter/MeterClient.js` เรียก `jsPDF.text()` + `jspdf-autotable` โดยตรงกับ font `helvetica` ซึ่งไม่รองรับ Unicode ไทย ทำให้ตัวอักษรไทยทั้งหมดโชว์เป็น `&&&` ใน PDF ที่ดาวน์โหลดออกมา — แก้โดยเปลี่ยนไปใช้ **html2canvas** (^1.4.1, ติดตั้งใหม่, dynamic-imported) แคปหน้า DOM ของ "ตัวอย่างใบแจ้งหนี้" ที่ browser render เอง แล้ว `pdf.addImage(canvas.toDataURL('image/png'), ...)` ลงบนหน้า A4; ผลคือ browser render ฟอนต์ Mali เต็มรูปแบบ ภาษาไทย/emoji/สีทุกอย่างสมบูรณ์ 100% ใน PDF; เพิ่ม `useRef` และ `invoiceRef` เข้า component + ปรับโครง `Print Invoice Modal` โดยแยก **input controls** (รายการหักเพิ่มเติม + จำนวน) ออกไปอยู่ **เหนือ** invoice preview เพื่อไม่ให้ input fields ถูกแคปเข้า PDF; สร้าง `<div ref={invoiceRef}>` เป็น A4-style invoice preview เต็มรูปแบบ — หัวกระดาษ "KLONGZAND PMS / ใบแจ้งหนี้ค่าเช่า", แถวข้อมูลห้อง+รอบบิล+วันที่พิมพ์, ตารางรายการ (bg-blue-600 header) รวมค่าเช่า/ค่าไฟ/ค่าน้ำ/รายการหักทุกแถว, กล่อง "รวมทั้งสิ้น" สีน้ำเงิน, footer text; html2canvas config `scale: 2, useCORS: true, backgroundColor: '#ffffff'` → คมชัด + พื้นหลังขาว; ถ้าภาพสั้นกว่าหน้ากระดาษจัด `yOffset` ให้กลางแนวตั้ง, ถ้ายาวเกินคลิปเท่าหน้า; `jspdf-autotable` **ยังอยู่ในโปรเจกต์** เพราะ `components/ExportButtons.js` (ใช้ในหน้า `/transactions` และ `/dashboard`) ยัง export ตารางแบบ text ผ่าน autoTable อยู่ — แค่ MeterClient ไม่ import แล้ว
- ✅ **หมวดหมู่รายรับ/รายจ่ายเปลี่ยนเป็น dynamic** (2026-07-03, admin จัดการเอง): เดิม `INCOME_CATEGORIES` และ `EXPENSE_CATEGORIES` ถูก hardcode ไว้ใน `components/TransactionForm.js` — ย้ายไปเป็นตาราง `transaction_categories` (id uuid, tx_type check-in `('income','expense')`, name text, sort_order int, is_active bool, created_at, unique(tx_type,name)) สร้างผ่าน `supabase_schema_transaction_categories.sql` + seed 4 หมวดรายรับ (ค่าห้อง/ค่ามัดจำ/ค่าบริการ/อื่นๆ) และ 5 หมวดรายจ่าย (ค่าสาธารณูปโภค/ซ่อมบำรุง/วัสดุ-อุปกรณ์/เงินเดือน/อื่นๆ) ให้เหมือน hardcode เดิมเป๊ะ; `app/(protected)/transactions/page.js` fetch categories (where is_active=true, order by tx_type,sort_order) เข้า Promise.all แล้ว split เป็น `incomeCategories`/`expenseCategories` (name-array) ส่งเข้า `TransactionForm` และ `TransactionsClient`; **usage counts** — สำหรับ admin fetch `select('category')` จาก transactions ทั้งหมด reduce เป็น `{ [categoryName]: count }` ส่งเข้า client เพื่อโชว์ในโมดัลจัดการหมวด (staff ไม่ต้อง); `TransactionForm.js` รับ `incomeCategories`/`expenseCategories` เป็น props แทน hardcode array — ถ้าหมวด tx_type ปัจจุบันว่างเปล่า แสดง notice สีอำพัน "แอดมินต้องเพิ่มก่อนใช้งาน" แทน `<select>`; ปุ่ม **"⚙ จัดการหมวดหมู่"** (btn-secondary) โผล่ในแถว actions ของ `/transactions` เฉพาะ admin — เปิด `components/ManageCategoriesModal.js` (client): แสดงลิสต์รายรับ/รายจ่ายพร้อม badge "(N รายการ)" ถ้ามี transactions ใช้อยู่, ฟอร์มเพิ่ม (select tx_type + input name + ปุ่ม "+ เพิ่ม"), ปุ่ม "✕" ต่อรายการเรียก `deleteTransactionCategory(id)` โดยถ้า usage > 0 confirm popup อธิบายว่า "รายการเดิมจะไม่ถูกลบ แต่หมวดหมู่นี้จะหายไปจากตัวเลือก" ก่อน (transactions.category เป็น text ไม่มี FK จึงไม่ cascade); server action `app/actions/transactionCategories.js` มี `addTransactionCategory({name, tx_type})` (trim, validate, auto assign sort_order = max+1, unique violation code 23505 → error "มีหมวดหมู่ชื่อนี้อยู่แล้ว") และ `deleteTransactionCategory(id)` — ทั้งสอง require role='admin' + revalidatePath('/transactions'); หมวดพิเศษที่ระบบสร้างอัตโนมัติ (เช่น 'ขายของ' จาก sellItem, 'ค่ามัดจำ'/'ค่าห้อง' จาก booking flow) **ไม่ได้ผูกกับตารางนี้** — ยังใช้ string ตรงๆ ใน server action เดิม ตารางนี้เป็นเพียงแหล่งข้อมูลของ dropdown ในฟอร์มแมนวลเท่านั้น
- ✅ **วิดเจ็ต "ติดตามการคืนทุน" ในหน้าแดชบอร์ด** (2026-07-03, admin only): การ์ดใหม่ระหว่างส่วน "ภาพรวมสะสมทั้งหมด" กับ "Occupancy เรียลไทม์" — ดึงข้อมูลจากตาราง `resort_settings` (id=1, ฟิลด์ `investment_cost`, `investment_start_date`, สร้างไว้แล้วผ่าน `supabase_schema_investment.sql`); แสดง **ต้นทุนลงทุนทั้งหมด** (จาก investment_cost), **กำไรสุทธิสะสม** (sum ของ transactions.tx_type='income' − 'expense' ตั้งแต่ investment_start_date ถึงวันนี้), **ยังขาดอีก X บาท** หรือ **"คืนทุนแล้ว!"** ถ้ากำไรสะสม ≥ ต้นทุน, **Progress bar** เปอร์เซ็นต์คืนทุน (สีน้ำเงินระหว่างคืนทุน / สีเขียวเมื่อคืนทุนครบ), และ **ประมาณการ "อีกประมาณ N เดือน"** คำนวณจากค่าเฉลี่ยกำไรสุทธิต่อเดือน 3 เดือนล่าสุด (bounded ด้วย investment_start_date ถ้าเริ่มนับหลังจากนั้น); ปุ่ม "✏ แก้ไขข้อมูลทุน" มุมขวาบนของการ์ดเปิด modal แก้ 2 ฟิลด์ (investment_cost + investment_start_date) → บันทึกผ่าน `updateResortSettings` server action; server action `app/actions/resortSettings.js` มี `getResortSettings()` และ `updateResortSettings({investment_cost, investment_start_date})` — updateResortSettings บังคับ role='admin' + validate cost ≥ 0 + start_date ไม่ว่าง + revalidatePath('/dashboard'); หน้าแดชบอร์ดขยาย query `allTimeTxs` ให้ select `tx_date` เพิ่มเพื่อ reuse ในการคำนวณ cumulativeNet (allTimeIncome/expense/net เดิมสำหรับการ์ด "ภาพรวมสะสมทั้งหมด" ยังคงเป็น all-time ไม่ filter start_date); component `components/InvestmentTracker.js` (client component ใช้ useRouter refresh) รับ props `investmentCost`, `investmentStartDate`, `cumulativeNet`, `avgMonthlyNet`; หน้าแดชบอร์ดเป็น admin-only ผ่าน middleware อยู่แล้ว จึงไม่ต้อง role check ในตัว component
- ✅ **กันเช็คอินซ้ำสร้างรายรับซ้ำ + confirm ก่อนเช็คอิน** (2026-07-03): บั๊กเดิม — กดปุ่ม "เช็คอิน" ในหน้า `/bookings` สองครั้งเร็วๆ ทำให้ `checkinBooking()` ใน `app/actions/bookings.js` insert transaction ค่าห้องซ้ำ (status update เป็น idempotent อยู่แล้ว แต่ insert transaction ไม่มี guard); แก้ 2 ชั้น: **(1) Server guard** ใน `checkinBooking()` — ก่อน insert เรียก `select('id').eq('booking_id', bookingId).eq('category', 'ค่าห้อง').limit(1)` ถ้ามี row อยู่แล้วข้าม insert ทั้งบล็อก (`remaining > 0` ยังเป็น outer guard เดิม); **(2) Client confirm** ใน `BookingsClient.js` — ขยาย signature `handleAction(action, id, booking = null)` ใส่ `window.confirm("ยืนยันเช็คอิน ห้อง {roomNo} — {customerName}?\nรายการนี้จะสร้างรายรับในระบบ")` ที่ต้นฟังก์ชันเฉพาะ `action === 'checkin' && booking`; ปุ่มเช็คอิน call-site ส่ง `b` เพิ่มเข้าไปเป็น arg ที่สาม; ปุ่ม checkout/cancel ไม่แตะ (default `booking = null` → ข้าม confirm block); **หมายเหตุ field alias** — query ใน `app/(protected)/bookings/page.js:14` ใช้ `customer:customers(...)` / `room:rooms(...)` เป็น singular alias ดังนั้น client ใช้ `b.customer?.full_name` / `b.room?.room_no` (ไม่ใช่ plural `b.customers` / `b.rooms`)

- ✅ **ช่อง "วันที่ / Date" ในใบเสร็จรับเงิน (ผู้ใช้เลือกเองได้)** (2026-07-03): เพิ่มคอลัมน์ `receipt_date` (type `date`) ในตาราง `receipts` (มีอยู่แล้วในสคีมา) เพื่อแยก "วันที่บนใบเสร็จ" ออกจาก `created_at` (เวลาที่บันทึกเข้าระบบ) — รองรับ backdate/postdate; `components/ReceiptForm.js` เพิ่ม state `receiptDate` (default `initialReceipt?.receipt_date ?? getTodayString()`) + ช่อง `<input type="date" required>` ใน card "ข้อมูลลูกค้า" เหนือช่องชื่อลูกค้า, ส่ง `receipt_date` เข้า payload; `app/actions/receipts.js` เพิ่ม helper `bangkokIsoDate()` (คู่กับ `bangkokDateStr()` เดิม, ต่างกันแค่คั่นด้วย `-` ให้เป็น `YYYY-MM-DD` แบบ Postgres date literal), `createReceipt` destructure `receipt_date` แล้ว insert `receipt_date: receipt_date || bangkokIsoDate()`, `updateReceipt` ใส่ใน `nextHeader` เป็น `receipt_date || current.receipt_date || bangkokIsoDate()`, และเพิ่ม `['receipt_date', 'วันที่ในใบเสร็จ']` เข้า `HEADER_FIELDS` array — diff mechanism เดิมจับการแก้วันที่และบันทึกลง `receipt_edits` timeline อัตโนมัติ; `components/ReceiptPreview.js` เปลี่ยน "วันที่ / Date" ในหัวใบเสร็จจาก `formatDate(receipt.created_at)` → `formatDate(receipt.receipt_date ?? receipt.created_at)` (fallback กัน row เก่าที่ receipt_date=NULL); **timeline "สร้างใบเสร็จ" ไม่แตะ** ยังใช้ `formatDateTime(receipt.created_at)` เพราะเป็นเวลาบันทึกจริง; `app/(protected)/receipts/ReceiptsClient.js` คอลัมน์ "วันที่" ใน list เปลี่ยนเป็น `formatDate(r.receipt_date ?? r.created_at)`; `getReceipt` และ `getReceipts` ใช้ `select('*')` อยู่แล้ว จึงได้ `receipt_date` มาโดยอัตโนมัติไม่ต้องแก้ query

- ✅ **ปุ่ม "🖨 ปริ้น" ใบยืนยันการจองในหน้า `/bookings`** (2026-07-03): เพิ่มปุ่ม `bg-teal-600` ใน `BookingsClient.js` — **แถวรายการจอง** (ต่อจากปุ่ม เช็คอิน/เช็คเอาท์/ยกเลิก/แก้ไข/ลบ ก่อน icon 📝📎) และ **cell popup** (คู่กับ "✏ แก้ไขการจองนี้"); ใช้ได้กับทุก booking ไม่จำกัดสถานะ ทั้ง admin+staff; state ใหม่ 2 ตัว: `printBookings` (data), `printLoading` (id ที่กำลังโหลด — disable ปุ่มและแสดง `...`); ฟังก์ชัน `handlePrint(bookingId)` เรียก `getBookingsByIds([bookingId])` (server action เดิมจาก `app/actions/bookings.js`) แล้ว setPrintBookings ทำให้ overlay โผล่; รีใช้ component `BookingPrintPreview` เดิมที่มีอยู่แล้ว (เดิมโผล่หลัง create booking เท่านั้น) — ครอบด้วย modal overlay `fixed inset-0 bg-black/50 z-50` (max-w-5xl, max-h-95vh, overflow-y-auto) คลิก backdrop หรือปุ่ม "ปิด" ใน preview เพื่อปิด; PDF export ยังทำงานผ่าน `handlePrint` ในตัว `BookingPrintPreview` (html2canvas + jspdf, multi-page split, ID card image ถ้ามี)

- ✅ **ถอดฟีเจอร์ "ปิดยอดประจำวัน" ออกทั้งระบบ** (2026-07-02): ลบฟังก์ชัน `closeDailyTransactions` และ block เช็ค `daily_closings`/`is_closed` ใน `app/actions/transactions.js` (createTransaction เพิ่มรายการได้ตลอด, deleteTransaction ลบได้ตาม role RLS); ลบ `components/DailyCloseButton.js` ทิ้งทั้งไฟล์; หน้า `/transactions` เอาปุ่ม "ปิดยอดประจำวัน"/"ปิดยอดแล้ว", คอลัมน์สถานะ, ปุ่มลบสีส้ม, popup พิเศษเรื่อง closed tx ออกทั้งหมด — ทุก row แสดงปุ่มลบสีแดงเหมือนกัน; `app/(protected)/transactions/page.js` เลิก query `daily_closings`; `BookingsClient.js` ตัด `hasClosedTx` (edit modal warning, delete confirm variant, badge 🔒) ออก; `cancelBooking` และ `adminDeleteBooking` ใน `app/actions/bookings.js` ตัด `.eq('is_closed', false)` — ลบทุก transaction ที่ผูก booking ไม่ว่า is_closed ค่าใด; `app/(protected)/bookings/page.js` เอา `is_closed` ออกจาก transactions select; ตาราง `daily_closings` และคอลัมน์ `transactions.is_closed` **ยังคงอยู่ในฐานข้อมูล** ตามที่ผู้ใช้กำหนด (ไม่แตะ DB)